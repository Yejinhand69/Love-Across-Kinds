using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;

public class RhythmGameInput : MonoBehaviour
{
    public RhythmGameButton rhythmGameButton;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        // check if touch down is hitting a rythmgamebutton and do stuff
        // check if touch up is hitting a rythmgamebutton and do stuff
    }
}
